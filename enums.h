#ifndef ENUMS
#define ENUMS


enum Direction {
    North,
    South,
    East,
    West,
    Up,
    Down,
    DirectionCount
};

enum class MoveType {
    Walk,
    Run,
    Crawl
};


#endif // ENUMS

